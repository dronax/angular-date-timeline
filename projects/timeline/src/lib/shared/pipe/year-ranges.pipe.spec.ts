import { YearRangesPipe } from './year-ranges.pipe';

describe('YearRangesPipe', () => {
  it('create an instance', () => {
    const pipe = new YearRangesPipe();
    expect(pipe).toBeTruthy();
  });
});
